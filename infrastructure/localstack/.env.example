# =============================================================================
# LocalStack Configuration Template
# =============================================================================

# Services to enable (comma-separated list)
SERVICES=dynamodb

# Debug logging level (0=off, 1=on)
DEBUG=1

# LocalStack data directory
DATA_DIR=/tmp/localstack/data

# Additional LocalStack configuration
# LAMBDA_EXECUTOR=docker
# START_WEB=0
# PERSIST_DATA=1

# =============================================================================
# End of configuration
# =============================================================================